# Copy this file to .env at the same folder (feed_frontend/.env) and fill in your values.
# IMPORTANT: After creating or changing .env, you MUST stop and restart the dev server (npm start).
# These variables are read at build/start time by Create React App.

REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_KEY=your-anon-key
